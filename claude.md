Input information: Github cannot store the videso inside images/

Instructions: Use a CDN (could be youtube links) to display the videos. If relying on links, ensure that perhaps somewhere in the javascript code there is a beautiful function with the array of links and I will add them in one by one. The linking from one section to another inside the website is still quite important
